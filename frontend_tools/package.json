{
    "name": "plus500BitcoinMonitor_meteor",
    "private": true,
    "scripts": {
        "start": "meteor run",
        "visualize": "meteor --production --extra-packages bundle-visualizer"
    },
    "dependencies": {
        "@babel/runtime": "^7.16.3",
        "meteor-node-stubs": "^0.4.1"
    }
}
